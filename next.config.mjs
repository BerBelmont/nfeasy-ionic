/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
// Next.js configuration stub — project migrated to Ionic React + Vite.
// The previous Next.js app was migrated/removed. This file is left as a placeholder
// to make it explicit that Next.js is no longer used. Safe to delete.

/**
 * If you see this file, the repository was migrated to Ionic React.
 * Remove this file and the `app/` folder when you are ready to delete Next artifacts.
 */

export default {};